<% if @task.nil? %>
  <h1> 404 Not found</h1>
<%else%>
  <h2><%= @task.name %></h2>
  <p><%= "completion_date: " + @task.completion_date %></p>
  <p><%= "description: " + @task.description %></p>
<%end%>
